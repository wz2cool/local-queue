package com.github.wz2cool.localqueue.model.enums;

/**
 * consume from where
 *
 * @author frank
 */
public enum ConsumeFromWhere {
    /**
     * the first existing excerpt in the queue
     */
    FIRST,
    /**
     * the last existing excerpt in the queue
     */
    LAST,
}
